// import * as chai from 'chai';
// import chaiHttp from 'chai-http';

const chai = require('chai');

const { expect } = chai;
// chai.use(chaiHttp);

  describe('Ok, ok', () => {
    it('joia', () => {
      expect('des').to.eql('des');
    });
  });
